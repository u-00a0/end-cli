{
  "name": "end-web-spa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:e2e": "npm run build:wasm && npm run build && npm run preview -- --host 127.0.0.1 --port 4173 --strictPort",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:console": "playwright test console-errors.spec.ts",
    "build:wasm": "bash ../scripts/build_web_wasm.sh"
  },
  "dependencies": {
    "@dagrejs/dagre": "^2.0.4",
    "@xyflow/svelte": "^1.1.3",
    "material-symbols": "^0.40.2",
    "smol-toml": "^1.6.0",
    "svelte": "^5.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@playwright/test": "^1.51.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.4",
    "svelte-check": "^4.1.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vitest": "^4.0.18"
  }
}
